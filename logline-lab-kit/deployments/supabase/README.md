# deploy/supabase (config refs only)
Supabase deployment configuration. Holds project URL and the **env var name** of
the service key (a reference), never the key itself. Apply
`profiles/supabase/migrations/` to the project. GHOST `supabase-live-ingest`.
