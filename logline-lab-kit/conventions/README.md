# conventions

The Convention Table: shared vocabulary (did types, scopes, statuses) that packs
extend. GHOST `Convention Table schema` (build-pack 06). Packs declare their
vocabulary in their `pack.json` (`did_vocabulary`, `scopes`); core does not
hardcode pack vocabulary.
