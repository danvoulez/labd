# generator
Scaffolding generator for new Labs/packs/profiles. v0 ships the example manifests
under `examples/manifests/` and `profiles/`/`packs/` as templates; a dedicated
generator binary is a future addition.
