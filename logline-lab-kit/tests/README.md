# tests
Acceptance gates A1-A30 are implemented as in-crate unit tests plus the
end-to-end `crates/logline-lab-acceptance` harness (A23-A30). Run `cargo test`.
See `recovery/ACCEPTANCE_STATUS.md` for the verified mapping.
