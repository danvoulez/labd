# pitwall-adapter (non-core, GHOST)

Bench integration surface. GHOST `apps/pitwall`: depends on the external Pitwall
app, not present here. Closure: wire to a real Pitwall instance reading
projections only (no core dependency).
