# mcp-server

Generic MCP/app authority boundary, implemented in Rust
(`logline-lab-mcp-server`). A tool call becomes a draft Act for the Lab to admit;
unregistered/ungranted clients are rejected. GHOST `mcp-server-as-ts`: a parallel
TypeScript surface is an open decision (FINAL §23).
