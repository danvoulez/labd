# cockpit-adapter (non-core, GHOST)

UI / projection integration. The cockpit registers through the MCP/app boundary
(`packages/mcp-server`) and reads projections. GHOST `apps/cockpit`: external UI
not present here. Closure: connect a real cockpit; verify it can only draft Acts
and read projections.
