# model-middleware (non-core, GHOST)

AI SDK wrapper: translate language into candidate Acts, suggest slots, critique
evidence gaps. It is NOT authority and NOT evidence (A28). The boundary it would
enforce already lives in `crates/logline-lab-core::app`; the model surface is
deferred. GHOST `model middleware package boundary`.
