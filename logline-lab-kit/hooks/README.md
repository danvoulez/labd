# hooks

Hook definitions run by `crates/logline-lab-hooks`. Hooks react to Lab events
(e.g. a daily report on a clock tick). A hook is a reaction, not authority: it
cannot close scopes or assert truth.
