# runtimes (non-core)

Workers are the hands. A runtime executes admitted workorders and returns
evidence; it never defines semantic truth (Operator §10, A19). The generic worker
contract lives in `crates/logline-lab-dispatch`.
