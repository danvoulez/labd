# local-worker

The reference worker lives in `crates/logline-lab-dispatch` (`ShellWorker`):
allow-gated, dry-run + real modes, returns evidence not closure (A29). This is the
deployment home for a standalone local-worker binary.
