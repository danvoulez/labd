# shell-worker

The reference worker is implemented in `crates/logline-lab-dispatch`
(`ShellWorker`): allow-gated, dry-run + real modes, returns evidence not closure.
This directory is the deployment home for a standalone shell-worker binary.
