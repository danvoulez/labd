# manhattan runtime (non-core, GHOST)

macOS implementation of the Manhattan fleet runtime. GHOST
`runtimes/manhattan macOS install`: no macOS host in this environment. Closure:
install on a LAB machine and capture runtime evidence. Manhattan is a pack +
runtime, never the product root (Operator §13).
