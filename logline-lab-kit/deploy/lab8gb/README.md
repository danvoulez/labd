# deploy/lab8gb (non-core, GHOST)
Deployment config for the lab8gb LAB machine. These values are deployment-specific
and must NOT be hardcoded into Lab Kit core (Operator §13). GHOST: per-LAB
Ethernet interface name (G-08) and package installation state.
