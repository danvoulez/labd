# Manhattan L-06

L-06 is the serious proof fixture (FINAL §18): a real future obligation carried
through the full chain — Act → schedule → due → gate → worker probe → evidence →
scoped receipt → projection/report — with ghosts where proof is missing.

Proof chain and acceptance:
- A45 L-06 exists as Acts in `packs/manhattan` (scope `manhattan.L-06`).
- A46 scheduled as a future obligation; A47 reached when due (ruler).
- A48 gate admits/blocks with an explicit reason.
- A49 worker runs a real probe when allowed; A50 evidence captures the result.
- A51 receipt candidate closes only `manhattan.L-06`.
- A52 report records what changed and what remains ghosted.

GHOST `G-08`: the real fleet Ethernet ping (per-LAB interface) is not performed
here. v0 proves the **mechanism** with a genuine local subprocess; closing G-08
requires confirming the interface on a LAB machine and capturing a real peer ping.
