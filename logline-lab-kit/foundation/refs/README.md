# foundation/refs
Pointers to Foundation protocol references used during recovery. The canonical
JSON + tuple/content hash algorithm in `crates/logline-act/src/canonical.rs` was
recovered (rewritten standalone) from the Foundation `status` crate.
