# Conformance vectors (for other Labs)

Valid reference Acts another Lab can run and compare against. The kit's own
runner (`crates/logline-lab-conformance`) embeds these and exports each with a
deterministic `content_hash` (A15/A16). Full vector set (valid/invalid/ambiguous)
lives in `foundation/conformance/`.
