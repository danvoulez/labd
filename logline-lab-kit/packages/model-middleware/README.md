# model-middleware (non-core, GHOST)

AI SDK wrapper. Captures claims, applies redaction, records a canon verdict — it
is **not** truth (Operator §10). GHOST `model middleware package boundary` +
`mcp-server-as-ts`. The authority boundary it would enforce already lives in
`crates/logline-lab-core::app`; the model surface itself is deferred.
